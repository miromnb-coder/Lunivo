import type { AgentToolDefinition } from './types.ts';

export function getAvailableTools(): AgentToolDefinition[] {
  // Placeholder for future agent tools.
  // Examples: search chat history, analyze attachments, create quizzes, build study plans.
  return [];
}
